name: personal_flow
description: Aplicativo de tarefas inteligentes.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  
  # rxdart: ^0.22.6
  expandable: ^3.0.1
  provider: ^3.2.0
  # bloc_pattern: ^2.5.1

  cupertino_icons: ^0.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:
  uses-material-design: true